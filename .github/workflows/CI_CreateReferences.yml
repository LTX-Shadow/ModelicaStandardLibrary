name: CI_CreateReferences

on:
  pull_request:
    types: [labeled]

permissions:
  contents: read
  
jobs:
  run:
    runs-on: ubuntu-latest
    if: ${{ github.event.label.name == 'ref-result' }}
    steps: 
      - uses: actions/github-script@v9
        with:
          github-token: ${{ secrets.LTX_SIMULATION_TOKEN }}
          script: |
            const prNumber = context.payload.pull_request?.number ?? context.payload.number;

            const { data: pr } = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber,
            });

            const labels = (pr.labels || []).map(l => typeof l === 'string' ? l : l.name);
            const hasLabel = labels.includes('secure-c-code');

            await github.rest.repos.createDispatchEvent({
              owner: '${{ secrets.PRIVATE_CI_OWNER }}',
              repo: '${{ secrets.PRIVATE_CI_REPO }}',
              event_type: 'RefCI_trigger',
              client_payload: {
                number: prNumber,
                head_sha: pr.head.sha,
                base_sha: pr.base.sha,
                has_label: hasLabel,
                github_repo: context.repo.full_name
              }
            });
